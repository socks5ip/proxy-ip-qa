# 判断代理IP是否「干净」：四个信号

平台判断你的 IP 是不是代理，靠的不是单一指标。四个信号里任何一个不达标，都可能导致被限流、被验证、被封。

## 信号一：ASN 归属（最核心）

**ASN（自治系统号）告诉平台这个 IP 属于哪一类网络。**这是权重最高的一项，因为它最难伪造。

- 属于**家庭宽带运营商** → 被当作自然用户
- 属于**数据中心 / 云厂商** → 大概率被标注为机房或代理

命令行直接查：

```bash
curl "http://ip-api.com/json/1.1.1.1?fields=status,country,isp,as,proxy,hosting"
```

看返回的 `as`（ASN 名称）里是否出现 `Hosting`、`Datacenter`、`Cloud`、`VPS` 等关键词。

## 信号二：IP 类型数据库标注

第三方数据库会把 IP 分类为 residential / hosting / proxy / mobile 等。**多个库交叉验证**比单一来源可靠——某个库标错是常见现象。

## 信号三：黑名单与风险评分

这个 IP 是否出现在滥用、垃圾邮件、欺诈类数据库中。理想状态下应当**零命中**。

常见排查渠道包括公开的 DNSBL 与商业风控库。注意：住宅 IP 被前任用户「用脏」后也会进黑名单，所以**换 IP 比投诉更快**。

## 信号四：DNS 与 WebRTC 泄漏

前三个信号说的是 IP 本身，这一个说的是**你会不会暴露真实 IP**：

- **DNS 泄漏**：流量走了代理，但域名解析仍由本地运营商完成 → 出口暴露
- **WebRTC 泄漏**：浏览器通过 WebRTC 直接暴露真实 IP，绕过代理

测试方法：把代理配置好后，访问任意 IP 查询页，确认显示的**只有**代理 IP，且 DNS 解析走的是代理侧。

## 四项快速自查清单

```
□ ASN 是否属于家庭宽带（而非 Hosting / Cloud / Datacenter）
□ 多个 IP 类型数据库是否都标为 residential
□ 是否不在任何滥用 / 欺诈黑名单
□ 是否无 DNS 与 WebRTC 泄漏
```

四项全过才算干净。**只换 IP 不改其他环境**（浏览器指纹、行为模式），在强风控平台往往效果有限。

## 关于「纯净度分数」

不少工具会给你一个 0–100 的分数。分数可以看趋势，但**不要只看分数**——它本质是上面几项的加权，且不同工具算法不同。**要看具体是哪一项拖后腿，才能对症解决。**

## 一个实用原则

**新拿到的 IP 先测再用。**正规平台基本都提供免费测试，把 ASN、类型、黑名单、泄漏四项都过一遍，再决定是否长期采购。这一步花三分钟，能省掉后面很多麻烦。

---

免费 IP 综合检测中心（ASN 归属 / 纯净度 / 线路延迟 / 连通性）：https://socks5ip.com.cn/ip-check-center/

住宅 IP 各平台档位与价格：https://socks5ip.com.cn/jiagezhongxin/

*最后更新：2026-09-16*

---

> 本文来自 [全网低价IP](https://socks5ip.com.cn)（代理IP服务商聚合比价平台）。价格与政策以各平台官方页面为准，最后更新：2026-09-16。
